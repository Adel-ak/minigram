scalar Upload

type Query {
  _empty: String,
}

type Subscription {
  _empty: String,
}

type Mutation {
  _empty: String,
}

schema {
  query: Query
  subscription: Subscription,
  mutation: Mutation,
}
